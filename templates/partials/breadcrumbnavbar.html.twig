<nav class="breadcrumb">
    {% for item in items %}
        {% if not loop.last %}
            <a href="{{ item.url }}">{{ item.label }}</a>
            <span>›</span>
        {% else %}
            <span>{{ item.label }}</span>
        {% endif %}
    {% endfor %}
</nav>