# Jeudi 26 Mars 2026

## A faire

>Récupérer toutes les balises HTML dans documentation 'balisesHTML.md'
>
>Vue Twig à modifier
>
>CSS à mettre en place bien sur!
>
>Controlleur à controller

## Doc balises HTML

Ajout fichier `balisesHTML.md` dans `/Doc`

WTF la mise en page? bref

## Vue twig

On va organiser la liste de présentation avec des cartes pour faire propre.

Ici le but est de juste présenter en un coup d'oeil les infos pour ensuite se diriger sur la fiche de la planète concernées.

On utilise un maximum de balises pour faire qlqchose de bien et on s'attaque au CSS par la même occasion.

> ## NB
>
> Petit soucis car symfony/asset n'était pas mis!
>
> Alors forcement ça foire tout, on l'installe puis on vide le cache:

```bash
composer require symfony/asset
./composer.json has been updated
Running composer update symfony/asset
Loading composer repositories with package information
Updating dependencies
Lock file operations: 1 install, 0 updates, 0 removals
  - Locking symfony/asset (v5.4.45)
Writing lock file
Installing dependencies from lock file (including require-dev)
Package operations: 1 install, 0 updates, 0 removals
  - Downloading symfony/asset (v5.4.45)
  - Installing symfony/asset (v5.4.45): Extracting archive
Package doctrine/annotations is abandoned, you should avoid using it. No replacement was suggested.
Package doctrine/cache is abandoned, you should avoid using it. No replacement was suggested.
Package symfony/security-guard is abandoned, you should avoid using it. No replacement was suggested.
Generating optimized autoload files
composer/package-versions-deprecated: Generating version class...
composer/package-versions-deprecated: ...done generating version class
98 packages you are using are looking for funding.
Use the `composer fund` command to find out more!

Run composer recipes at any time to see the status of your Symfony recipes.

Executing script cache:clear [OK]
Executing script assets:install public [OK]

No security vulnerability advisories found
```

```bash
php bin/console cache:clear

 // Clearing the cache for the dev environment with debug true                                                          

                                                                                                                        
 [OK] Cache for the "dev" environment (debug=true) was successfully cleared.
```

On ajoute le dossier `assets` dans `public` qui contiendra donc `images` ainsi que `CSS`. On en profite pour faire le favicon et l'inclure.

Pour la présentation de notre liste planètes on aura le CSS `planets.css` et pour la planète spécifique ce sera `planet.css` tout simplement. On oublie pas de rajouter "assets" dans les pa`ths des vues twig bien sur!

Aucun soucis avec nos controlleurs et après petites rectifications on a un bel affichage simple et éfficace!

## A finir

Convertir photos `jpg` en `webp` et les rédimensionner