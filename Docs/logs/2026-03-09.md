# Lundi 9 Mars 2026

Un petit gars est malade c'est compliqué...

## Résolution problème clé API

AU dernier épisode nous étions bloqué par une erreur 401 car nous avons oublié de renter la clé API!

Déjà on commence par ajouter tout de suite notre clé `SOLAR_SYSTEM_API_KEY` a `.env.local`:

```bash
###> Service PlanetService ###
SOLAR_SYSTEM_API_KEY="**********"
```

On continue par configurer le fichier YAML `config/services.yaml`. On y ajoute le pareamètre et l'argument de notre service:

```yaml
parameters:
    # Clé stockée dans le fichier .env.local
    solar_system_api_key: '%env(SOLAR_SYSTEM_API_KEY)%'

services:    
    App\Service\PlanetService:
        arguments:
            # variable qui va stocker notre clé via le paramètre
            $solarSystemApiKey: '%solar_system_api_key%'
```

On reprend donc ensuite notre Service concerné `PlanetService` et on ajoute une nouvelle variable pour stocker la clé API et l'utiliser lors de l'appel HTTP:

```php
<?php

namespace App\Service;

use Symfony\Contracts\HttpClient\HttpClientInterface;

class PlanetService
{
    private HttpClientInterface $client;
    private string $apiKey;

    public function __construct(HttpClientInterface $client, string $solarSystemApiKey)
    {
        $this->client = $client;
        $this->apiKey = $solarSystemApiKey;
    }

    public function fetchPlanets(): array
    {
        $response = $this->client->request(
            'GET',
            'https://api.le-systeme-solaire.net/rest/bodies',
            [
                'headers' => [
                    'Authorization' => 'Bearer ' . $this->apiKey,
                ],
            ]
        );

        $data = $response->toArray();

        return array_filter($data['bodies'], function ($body) {
            return isset($body['isPlanet']) && $body['isPlanet'] === true;
        });
    }
}
```

Cette méthode permet d'appeler l'API du système solaire, d'envoyer la clé d'authentification dans l'en-tête HTTP et de récupérer uniquement les objets correspondant aux planètes (isPlanet = true).

Verification du service via Symfony :

```bash
php bin/console debug:container App\Service\PlanetService

Information for Service "App\Service\PlanetService"
===================================================

 ---------------- --------------------------- 
  Option           Value                      
 ---------------- --------------------------- 
  Service ID       App\Service\PlanetService  
  Class            App\Service\PlanetService  
  Tags             -                          
  Public           no                         
  Synthetic        no                         
  Lazy             no                         
  Shared           yes                        
  Abstract         no                         
  Autowired        yes                        
  Autoconfigured   yes                        
 ---------------- --------------------------- 


 ! [NOTE] The "App\Service\PlanetService" service or alias has been removed or inlined when the container was compiled.
```

## Mise en BDD des données obtenues

On va faire un truc hyper stylé maintenant. On va mettre en BDD les données récupérées de l'API. accordion

On utilisera juste les données de la BDD (plus rapide et fiable) et l'appel à l'API sera utilisé pour faire les mise à jour des données en BDD! 

ENORME!! Let's go!

On fait ue nouvelle méthode `updatePlanetFromApi` dans `PlanetService` qui va récupérer les planètes depuis l’API, retrouver les planètes déjà présentes en base de données, mettre à jour leurs champs et sauvegarder les modifications!!

On a besoin d'injecter le repo et le entity mnager interface entre autre ce qui donne une fois fini:

```php
<?php

namespace App\Service;

use App\Entity\Planet;
use App\Repository\PlanetRepository;
use Doctrine\ORM\EntityManagerInterface;
use Symfony\Contracts\HttpClient\HttpClientInterface;

class PlanetService
{
    private HttpClientInterface $client;
    private string $apiKey;
    private PlanetRepository $planetRepository;
    private EntityManagerInterface $entityManager;

    public function __construct(
        HttpClientInterface $client, string $solarSystemApiKey, PlanetRepository $planetRepository, EntityManagerInterface $entityManager) 
    {
        $this->client = $client;
        $this->apiKey = $solarSystemApiKey;
        $this->planetRepository = $planetRepository;
        $this->entityManager = $entityManager;
    }

    public function fetchPlanets(): array
    {
        $response = $this->client->request(
            'GET',
            'https://api.le-systeme-solaire.net/rest/bodies',
            [
                'headers' => [
                    'Authorization' => 'Bearer ' . $this->apiKey,
                ],
            ]
        );

        $data = $response->toArray();

        return array_filter($data['bodies'], function ($body) {
            return isset($body['isPlanet']) && $body['isPlanet'] === true;
        });
    }

    public function updatePlanetsFromApi(): void
    {
        $apiPlanets = $this->fetchPlanets();

        foreach ($apiPlanets as $apiPlanet) {
            $planet = $this->planetRepository->findOneBy([
                'slug' => strtolower($apiPlanet['englishName'])
            ]);

            if (!$planet) {
                continue;
            }

            $planet->setEnglishName($apiPlanet['englishName'] ?? null);
            $planet->setMass(
                isset($apiPlanet['mass']['massValue'], $apiPlanet['mass']['massExponent'])
                    ? $apiPlanet['mass']['massValue'] * pow(10, $apiPlanet['mass']['massExponent'])
                    : null
            );
            $planet->setDiameter($apiPlanet['meanRadius'] ?? null);
            $planet->setGravity($apiPlanet['gravity'] ?? null);
            $planet->setDensity($apiPlanet['density'] ?? null);
            $planet->setMeanTemperature($apiPlanet['avgTemp'] ?? null);
            $planet->setSemimajorAxis($apiPlanet['semimajorAxis'] ?? null);
            $planet->setOrbitalPeriod($apiPlanet['sideralOrbit'] ?? null);
            $planet->setOrbitalSpeed(null);
            $planet->setMoonsCount(isset($apiPlanet['moons']) ? count($apiPlanet['moons']) : 0);
            $planet->setDiscoveredBy($apiPlanet['discoveredBy'] ?? null);
            $planet->setDiscoveredDate($apiPlanet['discoveryDate'] ?? null);
            $planet->setUpdatedAt(new \DateTimeImmutable());
        }

        $this->entityManager->flush();
    }
}
```

Pour tester tout ça il nous faut une nouvelle méthode avec une nouvelle route dans le `PlanetController` bien sur:

```php
    /**
     * @Route("/test-planets-sync", name="test_planets_sync")
     */
    public function testSync(PlanetService $planetService): Response
    {
        $planetService->updatePlanetsFromApi();

        return new Response('Planet sync completed');
    }
```